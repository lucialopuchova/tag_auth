module <%= @scope %>
  class TagAuthController < ApplicationController

    # protect_from_forgery with: :null_session, prepend: true
    # skip_before_action :authenticate_user!

    # GET /tag_auth/sign_in
    # def new
    #  TODO: insert the logic for rendering your sign in view
    #  raise NotImplementedError
    # end

    # POST /tag_auth/sign_in
    # def create
    #  @<%= @instance %> = <%= @model %>.find_by(auth_tag: <%= @instance %>_params[:auth_tag])

    #  if @<%= @instance %>
    #    token_assigner = TagAuth::TokenAssigner.new(@<%= @instance %>)
    #    token = token_assigner.assign_token

    #    render json: { success: true, token: token, email: @<%= @instance %>.email }
    #  else
    #    render :new, status: :not_found
    #  end
    # end

    # private

    # def <%= @instance %>_params
    #  params.require(:<%= @instance %>).permit(:auth_tag)
    #end
  end
end
