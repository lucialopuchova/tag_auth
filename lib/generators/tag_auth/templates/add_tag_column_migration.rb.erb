class AddTagTo<%= table_name.camelize %> < ActiveRecord::Migration<%= migration_version %>
  def up
    unless column_exists?(:<%= table_name %>, :auth_tag)
      add_column :<%= table_name %>, :auth_tag, :string
    end
  end

  def down
    remove_column :<%= table_name %>, :auth_tag
  end
end
