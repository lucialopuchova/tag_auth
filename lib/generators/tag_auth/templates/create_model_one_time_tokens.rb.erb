class Create<%= table_name.singularize.camelize %>OneTimeTokens < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :<%= table_name.singularize %>_one_time_tokens do |t|
      t.references :<%= table_name.singularize %>, foreign_key: true, index: true
      t.string :token, null: false
      t.datetime :expires_at, null: false

      t.timestamps
    end

    add_index :<%= table_name.singularize %>_one_time_tokens, :token, unique: true
  end
end
